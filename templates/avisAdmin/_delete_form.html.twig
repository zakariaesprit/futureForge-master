<form method="post" action="{{ path('admin_avis_delete', {'idAvis': avi.idAvis}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.idAvis) }}">
    <button class="btn">Delete</button>
</form>
